项目名称：AI 驱动的智能 IM 聊天系统（独立开发）
技术栈：Spring Boot 3.4 + Netty + FastAPI + LangGraph + Redis + MySQL + gRPC + Vue 3 + Electron

项目描述：基于 Spring Boot 3.4 + LangGraph 构建的 AI 驱动智能即时通讯系统，融合多阶段混合 RAG 检索、DAG 推理编排与断点容错、分层认知记忆及跨语言流式推理等核心能力，实现从意图理解到推理生成的智能闭环，可一站式完成智能问答、多轮深度对话与实时内容生成等多样化服务。

核心亮点

1. **LangGraph DAG 推理编排** — 11 节点有向无环图 + SQLite 节点级检查点实现断点容错，后处理串行 4.5s → 四路并行 1.8s（↓60%），异常恢复跳过已完成节点，节省 2-3 次重复 LLM 调用。

2. **多阶段混合 RAG 检索** — 两层意图分类器（正则 80% + 语义向量 99.5%）驱动 10 种意图差异化检索，BM25 + ChromaDB 双路召回 → 五维复合评分 → Cross-Encoder 精排，无效召回 ↓60%。

3. **分层认知记忆架构** — A/B/C 三层记忆体系（核心角色/摘要归档/结构化事件双写），n-gram Jaccard 跨层去重 + 冲突优先级裁决，200 轮压力测试验证 100% 记忆留存率。

4. **自研二进制通信协议** — 16 字节定长头部（Magic + Protobuf），Netty 半包粘包 + HTTPOnly JWT 鉴权 + Redis Pub/Sub 跨节点广播，单消息体积较 JSON ↓40%-60%。

5. **跨语言 gRPC 流式推理** — Java + Python 异构服务端流式 RPC 逐 Token 推送，StreamingTagParser 有限状态机处理标签截断，三级超时（30/60/120s）+ 失败自动降级 REST。
