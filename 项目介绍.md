项目名称：AI 驱动的智能 IM 聊天系统

技术栈：Spring Boot 3.4 + Netty 4.1 + FastAPI + LangGraph + Redis + MySQL + gRPC + Vue 3 + Electron

项目描述：独立设计并开发一款 AI 驱动的桌面端即时通讯应用，覆盖自研二进制通信协议、多阶段 RAG 检索、分层记忆系统、LangGraph 流水线编排、跨语言 gRPC 流式调用等核心模块。

核心贡献
1. 主导 AI 聊天主链路架构升级，将命令式线性函数重构为 LangGraph StateGraph 11 节点 DAG，引入节点级检查点实现断点容错

原 process_chat_msg() 数百行线性代码，LLM 超时需从头重试；基于 LangGraph 构建 START → PRE_FLIGHT → FETCH_MEMORY → PARSE_MEMORY → CLASSIFY_INTENT → [RETRIEVE | SKIP] → INVOKE_LLM → [SAVE_MESSAGE ∥ FORMAT_OUTPUT ∥ CLASSIFY_EMOTION ∥ TRIGGER_SUMMARY] 四路并行 → UNLOCK → END 有向无环图
条件路由：意图分类判定闲聊场景时跳过 RAG 检索直接调用 LLM；引入 SQLite-backed 节点级检查点（AsyncSqliteSaver），失败/重启后从断点恢复
后处理从串行约 4.5s 降至并行约 1.8s；异常恢复跳过已执行节点，平均节省 2-3 次重复 LLM 调用
2. 设计并实现多阶段混合 RAG 检索链路，基于意图驱动 scope 过滤 + BM25/向量双路并行召回 + 五维复合评分 + Cross-Encoder 精排

两层意图分类器：第一层正则规则（微秒级，覆盖 80% 高频指令），未命中 fallback 到第二层语义向量匹配（覆盖至 99.5%），10 种意图类型各自映射不同 event_type scope 与 top_k
双路并行召回：自研 BM25（jieba 分词 + Robertson-Sparck Jones IDF 平滑）+ ChromaDB 向量检索；五维复合评分公式 0.30×相关性 + 0.25×importance + 0.15×新鲜度 + 0.15×活跃度 + 0.15×特异性；FlashRank Cross-Encoder 精排 + 近 6 轮窗口关键词重叠去重
scope 过滤将无效召回降低约 60%；10 种意图类型覆盖身份/偏好/边界/关系/跟进/情绪/深度回忆等场景
3. 设计结构化分层记忆系统与统一 Prompt 组装器，通过 15 轮播种 / 200 轮干扰压力测试验证 100% 记忆留存率

构建 A2/B/C 三层记忆架构：A2 层存储用户硬边界约束（最高优先级），B 层为摘要归档写入 ChromaDB，C 层为结构化事件双写 Redis + ChromaDB
统一 Prompt 组装器执行跨层去重（字符 n-gram Jaccard 重叠度计算，A2→B→C 逐层抑制）、空块过滤、冲突优先级裁决；4 组独立 asyncio.gather 并行任务（摘要生成/A2 提取/事件压缩/画像更新）
自动化压力测试：12-15 轮播种注入多类记忆点 → 200 轮无关话题干扰 → 100% 记忆留存率
4. 设计并实现自定义二进制 WebSocket 通信协议，替代 JSON 文本协议

定义 16 字节定长头部（Magic 0xCAFEBABE 4B + Version 1B + Serial 1B + MsgType 2B + DataLength 4B + Reserved 4B）+ Protobuf 消息体
Netty 服务端实现 ProtocolMsgDecoder（markReaderIndex/resetReaderIndex 半包粘包处理 + Magic 校验）和 ProtocolMsgEncoder；WebSocket 升级阶段通过 CookieAuthHandler 从 HTTP Cookie 解析 HTTPOnly JWT 完成鉴权
结合 IdleStateHandler(40/50/120s) 心跳检测 + Redis Pub/Sub CHAT_CHANNEL 跨节点消息广播；单条消息体积相比 JSON 减少约 40-60%
5. 设计并实现跨语言 gRPC 流式调用框架，基于状态机实时标签解析实现 Java-Python 异构服务间逐 Token 流式传输

Java 端（Spring Boot）作为 gRPC 客户端，Python 端（FastAPI + grpc.aio）作为服务端，通过 rpc Chat(ChatRequest) returns (stream ChatResponse) 服务端流式 RPC 实现 AI 聊天的逐 Token 推送
自研 StreamingTagParser 有限状态机（TEXT/INSIDE_ACTION），解决流式场景下 LLM 输出的 <action> 标签跨 Token 边界问题：字符缓冲区 + 残缺标签前缀保留 + 安全截断策略，实时区分文本块与动作块
完善的连接生命周期管理：keepAlive(30s)/idleTimeout(60s)/deadline(120s) 三级超时控制 + gRPC 失败自动降级到 REST Feign 调用 + @PreDestroy graceful shutdown